<%- include('../partials/header') %>

<h1><%= category %></h1>

<% drinks.forEach(drink => { %>
 <a href="/drinks/reviews/<%= drink._id %>" class="category-drink-container">
    <div class="image"></div>
    <h2><%= drink.label %></h2>
    <div class="sub-copy">reviews: <%= drink.reviews.length %></div>
 </a>
<% }) %>

<%- include('../partials/footer') %>
